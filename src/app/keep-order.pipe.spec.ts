import { KeepOrderPipe } from './keep-order.pipe';

describe('KeepOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new KeepOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
